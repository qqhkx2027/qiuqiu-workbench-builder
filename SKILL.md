---
name: qiuqiu-workbench-builder
description: 把用户的个人效率、生活管理或内容创作需求，设计并实现为可离线使用的单文件 HTML 工作台。适用于数字工作台、待办、记账、习惯、备考、减脂、育儿和自媒体工作台；当用户希望手机电脑都能用、刷新不丢数据、可导出备份或部署分享时使用。默认遵循秋秋工作台方法：先从灵感模板开始，再用苏格拉底提问明确模块，最后完善细节和视觉主题。
---

# 秋秋工作台搭建技能

把一个模糊的“我想做个工作台”，变成一个可以直接打开、使用和分享的单文件 HTML。优先复用本技能的模板、构建脚本和验证流程，不凭空重新搭一套框架。

## 什么时候使用

- 用户想做个人工作台、仪表盘、待办、记账、习惯打卡、备考、减脂、育儿或自媒体管理页面。
- 用户说“像秋秋的工作台”“帮我搭一个 WorkBuddy 工作台”“我不会写代码，想做自己的工作台”。
- 用户要求手机和电脑都能用、离线可用、刷新后数据还在、可以导出备份或部署分享。

## 秋秋方法：从想法到工作台

1. **打开灵感**：先找一个接近需求的模板作为底板，不要求第一次就完全匹配，再围绕真实使用场景改造。
2. **苏格拉底提问**：当用户说不清楚模块时，先不要写代码。一次只问一个问题，直到能确定人群、目标、使用频率、信息来源和最重要的动作。可直接使用：

   > 现在的风格和排版我不太喜欢，请用苏格拉底提问法向我提问，一次只问一个问题，直到你完全理解我想要的工作台。

3. **完善细节**：将大类拆成可执行的子标签。例如“价值”可包含工作、学习、财务；“健康”可包含运动、饮食、睡眠、情绪；“生活”可包含家庭、朋友、体验、娱乐。只有确实会使用的模块才落地。
4. **调整美化**：最后再处理图标、颜色、主题和布局。用户提供参考 App 截图或喜欢的动漫形象时，先确认它要解决的是信息结构问题还是视觉问题。

文章《WorkBuddy 新手教程｜用提问方法，0 基础搭建专属你的 AI 工作台》中的典型拆分（灵感、提问、细节、美化）已整理到 [references/qiuqiu-workbench-method.md](references/qiuqiu-workbench-method.md)，需要设计工作台信息架构或撰写教程时读取它。

## 设计约束

- 首版最多 3–4 个核心模块；需求过多时先交付最常用的 3 个，再逐轮增加。
- 页面必须有“今天要处理”或等价的首屏入口，优先展示逾期、今天和临近到期事项。
- 首次打开不能是空白页：每个核心模块提供 3–5 条示例，其中至少 1 条能展示逾期或待处理状态，并提供“清空示例”。
- 数据默认只存浏览器 `localStorage`，每个工作台和每个主题使用独立前缀；不上传用户数据，不把真实隐私数据写入示例。
- 首屏提供 JSON 导出和导入恢复；清空数据必须二次确认。新增模块按 key 增量 seed，不覆盖老用户已有数据。
- 单文件 HTML 优先：CSS、JS、SVG、图标全部内联，不依赖 CDN、框架、远程字体或账号系统。图表用内联 SVG；导航图标优先使用图片或 SVG，emoji 只作为文案或状态提示。
- 移动优先：窄屏单列、按钮和点击区至少 44px、输入框字号至少 16px、表格可横向滚动或转成卡片，并处理底部安全区。
- 记账类遵循中国用户习惯：支出红色、收入绿色、货币使用 `¥`、日期使用 `YYYY-MM-DD` 或 `MM月DD日`。

## 标准工作流

1. **确认需求（最多两轮）**：确定人群、主要目标和最常用的 3 个模块；没有明确偏好时使用默认名称“秋秋工作台”和极简主题。
2. **选模板**：从 `assets/starter.html` 开始做新工作台；需要参考完整成品时读取 `assets/qiuqiu-workbench-source.html`。
3. **只改一个源文件**：源文件必须包含 `{{THEME}}`、`{{PREFIX}}`、`{{TITLE}}` 三个双花括号令牌。所有主题从同一份源文件生成，不能复制多份业务逻辑。
4. **构建和验证**：运行 `node scripts/build.js`。它会生成 `dist/` 下的主题变体，并用 Node `vm` 对每个变体和子标签执行渲染冒烟测试；必须看到 `ALL VARIANTS PASSED`。
5. **人工检查**：冒烟测试通过后，打开 `dist/index.html` 至少检查移动窄屏、导出、导入、清空确认、示例数据和主题图标。自动测试不能替代视觉检查。
6. **部署分享（用户要求时）**：部署整个 `dist/`，入口是 `dist/index.html`。返回根链接和各主题链接，并提醒用户手机浏览器打开后可添加到主屏幕。不要把本地 `file://` 链接当成分享链接。
7. **迭代**：每轮只新增一个模块或一组相关样式；保持旧 localStorage key 兼容，不清空已有数据。

## 主题和图标

主题由三部分组成，缺一不可：

1. `scripts/build.js` 中的变体配置（theme、独立 prefix、title）；
2. 源文件中的 `data-theme` CSS；
3. `assets/icons/icons.js` 中对应的图标集合。

更换三丽鸥或其他主题图标时，按顺序运行 `tools/generate_icons.py`、`tools/patch_icons.py`、`node scripts/build.js`。不要把远程图片 URL 写进成品 HTML；图片应裁剪、压缩后内联或放入项目 assets。

## 输出要求

完成后应交付：

- 可运行的 `dist/index.html` 和主题变体；
- 使用了哪些模块、数据是否本地保存、如何导出备份的简短说明；
- `node scripts/build.js` 的验证结果；
- 用户要求分享时，再提供部署后的 URL。

不要把实现过程、临时截图、系统缓存或无关的安装说明塞进技能包。技能的操作规则放在本文件，细节知识放在 `references/`，可执行逻辑放在 `scripts/` 或 `tools/`。

## 文件导航

- `references/standards.md`：完整设计标准、模块库、移动端和数据兼容规则。
- `references/qiuqiu-workbench-method.md`：秋秋文章提炼出的工作台方法、提问模板和示例信息架构。
- `scripts/build.js`：从单一源文件生成多主题并运行全变体冒烟测试。
- `assets/starter.html`：从零搭建轻量工作台的起始模板。
- `assets/qiuqiu-workbench-source.html`：9 个模块、4 套主题的完整生产范例。
- `tools/generate_icons.py`、`tools/patch_icons.py`：图标生成和注入流水线。
